[package]
name = "{{name}}"
version = "0.1.0"
edition = "2024"

[dependencies]
ruststream = { version = "0.2", features = ["macros", "json", "asyncapi", "logging"] }
ruststream-nats = "0.2"
serde = { version = "1", features = ["derive"] }
