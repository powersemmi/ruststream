[package]
name = "{{name}}"
version = "0.1.0"
edition = "2024"

[dependencies]
ruststream = { version = "0.3", features = ["macros", "memory", "json", "asyncapi", "logging"] }
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
